import type { FastifyPluginAsync } from "fastify";

/** Domain P — insurance programme, certificates, bonds and claims. */
export const insuranceModule: FastifyPluginAsync = async (app) => {
  void app;
};
