import type { FastifyPluginAsync } from "fastify";

/**
 * M11 — Independent benchmarking.
 * Metric registry, project metric snapshots, contribute-to-access
 * distributions with min-n suppression, percentile comparison signals.
 */
export const benchmarksModule: FastifyPluginAsync = async (app) => {
  void app;
};
