import type { FastifyPluginAsync } from "fastify";

/** Vol I §0.7 — webhooks off the ledger append path and OAuth2 machine callers. */
export const integrationsModule: FastifyPluginAsync = async (app) => {
  void app;
};
