import type { FastifyPluginAsync } from "fastify";

/** Domain W — mandatory lesson capture, retrieval, application and review. */
export const learningModule: FastifyPluginAsync = async (app) => {
  void app;
};
