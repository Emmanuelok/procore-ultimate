/**
 * agents — schema for the platform upgrade wave. Tables are added by the work
 * package that owns this area; keep every table company-scoped (company_id)
 * and project-scoped (project_id) where the record belongs to a project.
 */
export {};
